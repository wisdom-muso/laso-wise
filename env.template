# Django Settings
DEBUG=True
SECRET_KEY=g!y0otek@9t^b+b*7)&q2a5^=8_9&xcdii8@6h^_*wphl-(fu9

# Host Settings
ALLOWED_HOSTS=*,localhost,127.0.0.1

# CSRF Settings
# Include local dev and VPS if applicable
CSRF_TRUSTED_ORIGINS=http://localhost:8005,http://127.0.0.1:8005,http://65.108.91.110:12000

# Cookie Settings (set to True in production)
CSRF_COOKIE_SECURE=False
SESSION_COOKIE_SECURE=False

# Database Settings (if using external database)
# DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# Static Files
STATIC_URL=/static/
MEDIA_URL=/media/

# Email Settings (configure for production)
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your-email@gmail.com
# EMAIL_HOST_PASSWORD=your-app-password 